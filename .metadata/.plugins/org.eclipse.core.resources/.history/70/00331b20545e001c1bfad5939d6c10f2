package interaction;

public class Seller {
	/*판매자(Seller)
	//변수로 int money, int mango를 가집니다.

	생성자에서 money는 초기값이 0이고,  mango는 입력받도록 하세요


	sellMango ( int mango) 를 이용해서 망고개수를 입력받으면(doBattle과 유사)

	자신의 this.mango는 갯수만큼 차감하고 
	this.money는 망고개수 * 가격만큼 증가합니다

	망고가 모자라면 "망고가 모자랍니다" 메서드종료


	showSeller() 를 이용해 현재 소지금, 망고 개수를 볼수 있습니다.

	Buyer에서 망고개수를 확인할 수 있도록 getMango() 게터 하나만 만들ㅇ주세요*/
	private int money; // 매출액
	private int mango; //재고(개수)
	 
	
	public Seller(int mango) {
		this.money = 0;
		this.mango = mango;
		

	}
	
	/*public void sellMango(int  mango) {
		if (mango > 0 ) {
			this.mango -= mango;
			this.money += mango * 2000;
			System.out.println("망고의 갯수 : "+ mango + " 금액 : "+ this.money);
			
		} else { 
			System.out.println("망고가 부족합니다");
			
		}*/
	
	
	 public void sellMango(int mango) {
		 
		 if (this.mango < mango) {
			 System.out.println("망고가 모자랍니다. 현재 재고: "+ this.mango);
		       return;
		       
		 }
		 
		 this.mango -= mango;
		 this.money += mango*4000;
	}

	 public void showSeller() {
		 System.out.println("---------현재정보--------");
		 System.out.println("현재소지금: "+this.money + ", 현재망고재고 : "+ this.mango);
		 System.out.println("-----------------------");
		 
	 }
	//Buyer에서 망고개수를 확인할 수 있도록 getMango() 게터 하나만 만들ㅇ주세요
	public int getMango() {
		return this.mango;
	}
	 
	 
}
